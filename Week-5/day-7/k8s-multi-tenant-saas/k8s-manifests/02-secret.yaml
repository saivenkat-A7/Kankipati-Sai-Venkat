# Secret = a box of SENSITIVE settings (passwords, keys).
# Kubernetes stores these base64-encoded (NOT encrypted by default, just obscured).
# To create the base64 values yourself, run:
#   echo -n 'your_value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: saas-secret
  namespace: saas-app
type: Opaque
data:
  DB_USER: c2Fhc191c2Vy
  DB_PASSWORD: c2Fhc19wYXNzMTIz
  JWT_SECRET: c3VwZXJzZWNyZXRrZXk=
