apiVersion: v1
kind: ConfigMap
metadata:
  name: saas-config
  namespace: {{ .Values.namespace }}
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "{{ .Values.postgres.dbName }}"
  APP_ENV: "production"
